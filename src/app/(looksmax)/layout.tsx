import { LooksMaxRoot } from "@/features/app/shell/LooksMaxRoot";

export default function LooksMaxLayout({ children }: { children: React.ReactNode }) {
  return <LooksMaxRoot>{children}</LooksMaxRoot>;
}
